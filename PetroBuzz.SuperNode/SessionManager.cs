using System;
using Fleck;

namespace PetroBuzz.SuperNode
{
    public class SessionManager
    {
        internal void Register(IWebSocketConnection socket)
        {
            //throw new NotImplementedException();
        }

        internal void Unregister(IWebSocketConnection socket)
        {
            //throw new NotImplementedException();
        }
    }
}